namespace = information_events
## Events that fire to inform players of certain mechanics


country_event = {
	id = information_events.1
	title = information_events.1.t
	desc = information_events.1.d
	picture = BATTLE_eventPicture
	
	mean_time_to_happen = {
		days = 1
	}
	
	trigger = {
		ai = no
		NOT = { has_country_flag = player_knows_policies }
	}
	
	immediate = {
		set_country_flag = player_knows_policies
	}
	
	option = {
		name = information_events.1.a
		custom_tooltip = information_events.1.tt
	}
}

country_event = {
	id = information_events.2
	title = information_events.2.t
	desc = information_events.2.d
	picture = BATTLE_eventPicture
	
	# mean_time_to_happen = {
		# days = 1
	# }
	
	is_triggered_only = yes
	
	trigger = {
		ai = no
		always = yes
		#NOT = { has_country_flag = player_knows_limited_mission_trees }
	}
	
	# immediate = {
		# set_country_flag = player_knows_limited_mission_trees
	# }
	
	option = {
		name = information_events.2.a
		#custom_tooltip = information_events.1.tt
	}
	option = {
		name = information_events.2.b
		country_event = { id = information_events.3 } 
	}	
}

country_event = {
	id = information_events.3
	title = information_events.3.t
	desc = information_events.3.d
	picture = BATTLE_eventPicture
	
	# mean_time_to_happen = {
		# days = 1
	# }
	
	is_triggered_only = yes
	
	option = {
		name = information_events.3.a
		#custom_tooltip = information_events.1.tt
	}
}